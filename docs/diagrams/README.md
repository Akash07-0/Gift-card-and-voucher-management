# Diagram exports

`system-architecture.drawio` and `er-diagram.dbml` are editable source diagrams matching the current application model. PNG exports (`system-architecture.png`, `er-diagram.png`, and `class-diagram.png`) should be generated manually in diagrams.net or another diagram tool before submission.
